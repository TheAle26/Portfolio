/.env
.env
db.sqlite3
*.sqlite3

# Bytecode de Python (se regenera solo)
__pycache__/
*.pyc

# Secretos y datos sensibles: nunca deben ir al repo (menos si es público)
credenciales_prueba.txt
backup_datos.json
*.dump
*.sql

# Hashes de contraseñas de nginx (basic auth)
.htpasswd

# Grafo generado por graphify: se regenera con `graphify update .` (~2,8 MB, cambia siempre)
graphify-out/
